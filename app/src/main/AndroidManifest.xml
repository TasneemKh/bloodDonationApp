<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.blooddonationapp">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <uses-feature
        android:name="android.hardware.location.gps"
        android:required="true" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/red_wings_logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/red_wings_logo_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".updatingPassword"></activity>
        <activity android:name=".hospitalsPicker" />
        <activity android:name=".HistoryActivity" />
        <activity android:name=".TabActivity" />
        <activity android:name=".SplashMain" />
        <activity android:name=".Blood_type_Activity" />
        <activity android:name=".Add_Request_Activity" />
        <activity android:name=".datePicker" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps" />
        <activity android:name=".WelcomeActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".BloodBankSignIn" />
        <activity android:name=".CheckEmail" />
        <activity android:name=".forgetPassword" />
        <activity android:name=".signUp" />
        <activity android:name=".SignIn" />
        <activity android:name=".MainActivity" />
    </application>

</manifest>